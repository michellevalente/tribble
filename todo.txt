Class IP:

	Test if it is a valid IP

	You can test for certain properties of an IP address:
	isWildcard(), isBroadcast(), isLoopback(), isMulticast(), etc.

dns.hpp:

	dns_lookup()

Base_Socket:
	- get socket's address, peer's address, timouts, buffer size. (olhar funcoes da classe socket do python)

	- heranca: 
		UDP_Socket:
			- Funcao para multicast

		TCP_Socket:

Server:
	TCP/UDP server	

Reactor:

Talvez:

	Buffer: